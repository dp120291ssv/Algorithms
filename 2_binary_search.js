const randomArr = [1, 2, 5, 7, 9, 10, 12, 11, 3, 4, 6, 8];
let iterationCount = 0;

function binarySearch(arr, target) {
    let startIndex = 0;
    let endIndex = arr.length - 1;
}

binarySearch(randomArr, 4);
console.log('iterationCount', iterationCount);